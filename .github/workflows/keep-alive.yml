name: Keep Streamlit alive

on:
  schedule:
    - cron: "*/10 * * * *"  # elke 10 minuten

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sf --max-time 30 "https://prins-social-tracker.streamlit.app/?page=ping" || true
